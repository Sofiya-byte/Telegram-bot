# Telegram-bot

# Simple start:
```bash
# clone repo
git clone https://github.com/Sofiya-byte/Telegram-bot.git

# install requirements:
cd Telegram-bot/
pip install -r requirements.txt

# Create database, also you can specify the user_id you want to be an admin
python data.py

# Run bot
python main.py

```
